node_modules
.env
.env.local
dist
.vscode
.DS_Store
*.log
.vercel

# Service account key (local dev only — provide via GOOGLE_SERVICE_ACCOUNT_JSON in prod)
api/sa.json

# Local email preview (generated)
leaderboard-email-preview.html
